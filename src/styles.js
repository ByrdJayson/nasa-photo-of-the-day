import styled from 'styled-components';

export default function (){
 onst StyledImage = styled.img`
  width: 55%;
  margin:3rem;
  border: 5px solid black;

`

const StyledDiv = styled.div`
font-family: 'Poppins', sans-serif;


`
}
